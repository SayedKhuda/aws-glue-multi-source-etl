# Architecture

This folder contains the architecture diagram for the AWS Glue Multi-Source ETL project.

The pipeline reads Employee and Department CSV datasets from Amazon S3, joins the datasets using AWS Glue, performs data cleaning and transformation, and writes the processed data back to Amazon S3 in Parquet format.
